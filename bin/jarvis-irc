#!/usr/bin/env php
<?php
if (php_sapi_name() != 'cli') {
    die;
}

use jt2k\Jarvis\IRCBot;

require __DIR__ . '/../bootstrap.php';
$jarvis = new IRCBot($jarvis_config);
if (isset($argv[1]) && $argv[1] == 'debug') {
    $jarvis->setDebug(true);
}
$jarvis->connect();